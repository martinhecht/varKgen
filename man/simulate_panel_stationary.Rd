% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_panel_stationary}
\alias{simulate_panel_stationary}
\title{Simulate a stationary bivariate panel from a VAR(K) companion system}
\usage{
simulate_panel_stationary(N, T_obs, mats, P_stationary, seed = NULL)
}
\arguments{
\item{N}{Number of persons.}

\item{T_obs}{Number of time points (named \code{T} in the original script;
renamed to avoid masking \code{TRUE}'s shorthand).}

\item{mats}{List with companion matrices \code{F} and \code{Q}, e.g. from
\code{\link{make_mats_varK}} or the \code{mats} element of a
\code{\link{calibrate_varK}} result.}

\item{P_stationary}{Stationary state covariance, e.g. from
\code{\link{stationary_cov}} or the \code{P} element of a calibration
result.}

\item{seed}{Optional seed set before drawing (default \code{NULL}: the
current RNG state is used).}
}
\value{
A list with \code{N x T_obs} matrices \code{Y1} and \code{Y2},
  plus \code{psd_correction}, a named vector giving the positive
  semi-definiteness correction applied to \code{P_stationary} and to the
  process error block before factorization. Both entries are zero unless a
  negative eigenvalue at the level of numerical noise had to be truncated;
  a non-zero entry means the simulated model differs from the supplied
  matrices by exactly that amount.
}
\description{
Simulates \code{N} independent persons over \code{T_obs} time points from
the companion-form system \code{s_p = F s_{p-1} + eta_p}, starting each
person's state from the stationary distribution \code{N(0, P_stationary)}
so that no burn-in is required and all time points are stationary.
}
\details{
Compared to the original script, the multivariate normal draws use a
exact symmetric PSD factor computed once (for the initial state and once for the
2 x 2 process error covariance) instead of calling \code{MASS::mvrnorm} in
every time step (which performs an eigendecomposition per call). This
removes the MASS dependency and speeds up large simulations. Note that the
random number stream therefore differs from the original script for the
same seed; distributional properties are identical.
}
\examples{
m <- make_mats_varK(ax = c(0.5, 0.1), ay = c(0.4, 0.05),
                    b21 = c(0.2, 0.05), b12 = c(0.1, 0.02))
P <- stationary_cov(m$F, m$Q)
sim <- simulate_panel_stationary(N = 100, T_obs = 10, mats = m,
                                 P_stationary = P, seed = 1)
dim(sim$Y1)

}
